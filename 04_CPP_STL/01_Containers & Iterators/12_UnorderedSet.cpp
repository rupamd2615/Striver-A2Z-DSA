#include<bits/stdc++.h>
using namespace std;
int main(){
    //  not sorted but unique
    unordered_set<int> ust;
    // Lower bound and Upper bound functions don't work here
    // it does not have particular order but has a better complexity than set in most cases, except some when collision happens
    return 0;
}